"""終了コードの定義（application_checklist C-I01）。

個別の取得・評価・投入の失敗は異常終了とせず 0 で終える（REQ-F-010）。
非0になるのは**段階の全件が失敗した場合**のみで、取得側は `FETCH_ALL_FAILED`、
投入側は `INGEST_ALL_FAILED` に分ける（要件定義 §5 / F-001 AC-014 / F-004 AC-014）。
"""

OK = 0
"""正常終了。投入対象が 0 件の場合もこれに含まれる。"""

INGEST_ALL_FAILED = 1
"""投入対象が1件以上あったが全件失敗した（F-001 AC-014 / F-004 AC-016）。"""

CONFIG_ERROR = 2
"""**運用者が是正すべき**設定・秘匿情報の不備、または引数の誤り。

起動時検証で処理本体を開始する前に決定する（F-001 AC-030 / AC-032、SPEC-005 §5）。
実装起因の要求不正は `SPEC_ERROR` を用いる。
"""

STATE_PERSIST_FAILED = 3
"""状態の読み込み・書き込み・永続化に失敗した（F-002 AC-014 / AC-017 / AC-018）。

競合による失敗（AC-017）に限らず、書き込み不能・認証失効等も含む（AC-014）。

**同時に成立しうる他の失敗より優先する**（SPEC-005 §5）。記録に失敗すると
次回実行で全件が新規として扱われ、重複投入（R-002 違反）または再評価コストの
二重発生（REQ-NF-002a）を招くため、障害が次回へ伝播する唯一の経路である。
"""

SPEC_ERROR = 4
"""**実装が是正すべき**要求の不正。

LLM API が HTTP 400 を返した場合（プロンプト構成・スキーマ定義の誤り）に用いる
（SPEC-003 フロー #15）。`CONFIG_ERROR` と分けるのは、無人実行では終了コードが
原因究明の一次情報であり（F-002 AC-010）、同じ値に畳むと運用者が実装バグを
「自分の設定ミス」と誤読して調査を誤るため。
"""

FETCH_ALL_FAILED = 5
"""定義された情報源が1件以上あり、その**全件**の取得に失敗した（SPEC-001 フロー #18）。

供給が完全に途切れた状態であり、`INGEST_ALL_FAILED` と分けるのは、どちらの段階で
途切れたかを終了コードだけで判別するため（F-002 AC-010）。

**情報源の定義が0件の実行は含まない**（`OK`）。定義がなければ取得の失敗も起きておらず、
運用者が是正すべきは `feeds.yaml` の内容であって障害ではないため。

REQ-NF-003 は「投入0件の2回連続」を異常の副指標とするが、その検知は運用者がサマリを
見るまで働かない。全件失敗は供給停止が**確定した**状態で推測を要さないため、
非0で終了して GitHub Actions の失敗通知を発火させる。
"""

EVALUATE_ALL_FAILED = 6
"""評価対象が1件以上あり、その**全件**の評価に失敗した（TASK-099）。

**実地の dry-run で発見した**（2026-08-01）。API キーが無効で40件全滅したにも
かかわらず終了コードが `OK` であり、無人実行なら異常に気づけなかった。

`FETCH_ALL_FAILED` と同じ論理を評価段階に適用する — 供給が確定的に途切れて
おり推測を要さないため、非0で GitHub Actions の失敗通知を発火させる。
`SPEC_ERROR`（HTTP 400 = 実装の誤り）と分けるのは、こちらの主因が
**認証失効・API 障害という運用者側の是正対象**であるため。

**評価対象が0件の実行は含まない**（`OK`）。新着のない週を失敗扱いにしない
（F-001 AC-022 と同じ論理）。
"""
